
pure fun fact(n: int): int
{
    if (n == 0 || n == 1)
        return 1;
    else
        return n * fact(n - 1);
}

fun main()
{
    let i: int = 0;
    while (i < 20) {
        iprint(fact(i));
        i += 1;
    }
}
