
pure fun fib(n: int): int
{
    if (n == 0 || n == 1)
        return n;
    else
        return fib(n - 1) + fib(n - 2);
}

fun main()
{
    let i: int = 0;
    while (i < 20) {
        iprint(fib(i));
        i += 1;
    }
}
